# 慢行小城：正式改进路线图

本文用于维护《慢行小城》的产品、玩法与技术改进计划。完成状态必须与实际代码、关卡、玩家指南、管理员手册和测试保持一致；功能落地时应在同一任务中更新本文件。

## 状态说明

- ✅ 已完成：代码、文档和相应测试均已落地。
- 🟡 进行中：已有部分能力，但尚未达到本路线图的验收标准。
- ⬜ 待实施：尚未开始或只有讨论方案。
- ⏸ 暂缓：当前不进入近期开发计划。

## 当前基线

截至当前版本：

- 玩家内容为 3 个教学章节、17 个可自由选择的关卡，每章最后一关为连续五日挑战；另有管理员可见的隐藏设计素材库。
- 已实现道路等级、显式连接、剪断、道路引导、动态寻路、路口让行与信号灯、多线路公交和一次同站换乘。
- 已实现三项独立星级、个人最佳、运营前检查、持续规划提示、结算复盘、参考答案、撤销/重做、挑战/沙盒模式。
- 已实现带版本和严格校验的自动进度：可继续最近关卡及规划草稿，并完整恢复五日日期、收入、区域、支出和检查点。
- 已实现不遮挡地图的情境式新手引导：第一关按真实操作推进，新工具首次开放时给出单机制提示，并支持跳过和重新打开。
- 已实现自由选关下的推荐游玩路径：关卡显示状态与星级，章节汇总完成度，结算明确推荐下一课及学习重点。
- 五日任务已支持逐日收入、检查点回溯、条件建筑、付费区域开放和逐日参考答案链。
- 玩家端支持鼠标、触摸、键盘和屏幕阅读器可访问网格，并适配 320～1440px 视口与高 DPR Canvas。
- 关卡与核心保持确定性；当前 Node 测试、玩家浏览器冒烟测试和 UI 冒烟测试均覆盖主要流程。

## 已完成能力盘点

| 方向 | 状态 | 当前实现 |
| --- | --- | --- |
| 运营前检查与持续提示 | ✅ | 检查不可达住宅、目的地容量、公交有效性、理论送达上限、误降级与信号冲突；地图和任务面板可定位问题。 |
| 路径与人口解释 | ✅ | 住宅显示预计路径、目的地、瓶颈与人口守恒；目的地显示容量、预约与抵达。 |
| 寻路与主动分流 | ✅ | 自由流路径结合实时负载、受阻、路口等待和信号周期动态重估，并支持偏好与汽车禁行。 |
| 道路与路口深度 | ✅ | 三级道路、车道转向分工、无灯优先级、自动/手动信号、分阶段绿灯和冲突区预约均已实现。 |
| 公交系统 | ✅ | 支持多线路、班距、多车、双向停站、开放线路、站点客流、运营报告及一次同站换乘。 |
| 规划工具 | ✅ | 支持原子拖拽、剪断、矩形与整段道路操作、施工预览以及 60 步撤销/重做。 |
| 章节学习曲线 | ✅ | 已形成道路、路口、公交三章渐进式教学线，并隐藏不属于主线的设计素材。 |
| 正反馈与复盘 | ✅ | 已有贯通提示、送达里程碑、抵达粒子、居民情绪、彩片、星级揭晓、个人最佳及详细结算。 |
| 五日成长机制 | ✅ | 已有逐日收入、规划继承、条件建筑、付费区域、建设点支出、检查点回溯及答案链。 |
| 移动端与无障碍 | ✅ | 已有地图优先布局、底部抽屉、触摸坐标、键盘操作、减少动态效果和可访问网格。 |
| 共享关卡校验 | ✅ | 玩家加载、管理员 API 与 Node 测试共用结构化校验，并支持字段路径和地图格定位。 |
| 管理员发布流程 | ✅ | 已有鉴权、登录限速、草稿、撤销、试玩通关门禁、答案链验证、发布和服务重启。 |
| 自动化回归 | ✅ | 覆盖核心不变量、全部关卡参考方案、服务器安全、玩家/管理员浏览器流程及响应式布局。 |

# 近期路线

## P0：恢复与保留玩家进度

### 1. 完整的“继续游戏”能力 — ✅

规划修改、切关和进入新一天会自动保存最近游玩的关卡、挑战/沙盒模式及当前设计；五日任务同时保存日期、预算、逐日结果、区域开放、建设支出、参考方案分歧和全部已到达检查点。刷新后顶部提供一次点击继续入口。

自动进度采用独立 v1 封装，内部设计继续由 `City.loadDesign()` 严格校验并兼容 v1～v8；多日状态会先在独立候选会话中校验所有结果、区域、支出、当前设计和检查点，任何损坏均整体回退到新游戏。设计菜单可只清除此自动进度，不影响逐关手动设计、星级、个人最佳或参考答案解锁。Node 测试覆盖往返、旧设计加载路径、损坏/存储失败和原子回退，玩家浏览器冒烟测试覆盖真实刷新、继续及定向清除。

### 2. 情境式新手引导 — ✅

第一关地图上方会依次提示玩家亲自切换拖拽工具、连接全部同色建筑、用选择模式检查住宅预计路线，再自行开始运营；步骤由实际工具、连接和选择状态推进，不会代替玩家施工。引导位于 Canvas 外，不遮挡目标格，操作按钮只定位并聚焦对应控件，同时支持鼠标、触摸、键盘与屏幕阅读器。

道路等级、剪刀、路口控制和公交首次开放时只显示当前新机制。单击不施工、预算不足、未显式连接等问题继续通过地图内通知与规划问题标记就地解释。玩家可随时跳过，并从帮助弹窗重新打开；完成和已读机制只保存在本浏览器，减少动态效果设置会关闭相关过渡动画。纯逻辑测试覆盖步骤与机制顺序，浏览器回归覆盖首次显示、控件定位、跳过和重新打开。

### 3. 文档一致性修复 — ✅

README、游戏指南、帮助弹窗、工具栏 ARIA 标签和浏览器测试现已统一为：观察 1、选择 2、拖拽 3、剪刀 4、公交 5；Node 静态断言会检查各处映射及实际键盘处理，防止再次漂移。

## P1：加强推进感与复盘价值

### 1. 推荐游玩路径 — ✅

关卡选择器现在同时显示「未开始 / 进行中 / 已通关」和累计星级，并用非强制的「推荐」标记突出最近未完成关卡或顺序中的第一门未完成课程；全部可见关卡仍可随时进入。章节标签汇总已通关关数和总星数，自动进度存在时首次展开会优先定位最近关卡所在章节。

普通关卡与五日最终结算成功后会突出推荐下一课，并说明新开放的道路升级、剪刀、检查、路口控制、公交或连续多日机制；五日中途提示进入下一天，失败时则建议保留当前设计继续排查。状态与星级均来自本浏览器的严格、独立进度数据，不会用于锁关。纯逻辑测试覆盖状态、推荐和章节汇总，浏览器回归覆盖首次推荐、结算推荐及通关状态更新。

### 2. 地图化失败复盘 — ⬜

当前已有前三道路热点和路口/公交报告，但结算数据与地图之间联动不足。

计划：

- 结算热点、问题路线和路口报告可点击定位；
- 保留整轮累计等待/占用热力图，而不只显示瞬时负荷；
- 对比本次与个人最佳的满意度、P95、建设点和最大排队；
- 根据实际瓶颈生成有限、可解释的建议，不直接替玩家给出完整答案。

验收标准：失败后玩家能从结算页在两次点击内定位主要瓶颈，并能返回原设计继续修改。

### 3. 沙盒实验能力 — ⬜

当前沙盒不限时并允许运营中改路，但切换模式会清空设计，且仍沿用关卡预算。

计划：

- 支持把当前挑战方案复制到沙盒；
- 提供“沿用预算 / 无限预算”选项；
- 支持调整需求倍率、运营速度和持续饱和流量；
- 支持只重置交通状态而保留设计；
- 从沙盒返回挑战时不得静默覆盖挑战方案。

验收标准：玩家可用不超过三步把失败方案带入沙盒验证改造，不损坏原挑战进度。

### 4. 设计导入、导出与分享 — ⬜

在不增加服务器端账户的前提下，支持：

- 将当前设计导出为带版本的 JSON 文件或文本分享码；
- 从文件/文本导入并严格校验；
- 分享内容只包含规划数据，不包含成绩、车辆或管理员关卡定义；
- 导入前预览关卡 ID、道路数、公交线数和预计费用。

验收标准：合法设计可跨浏览器往返，恶意或损坏输入不会部分修改当前城市。

## P2：扩展长期可玩性

### 1. 确定性挑战变体 — ⬜

可逐步加入固定种子的挑战，不依赖后端或随机运行时状态：

- 每日/每周种子挑战；
- “禁止快速路”“最多一个信号路口”“公交限定”等规则修饰；
- 以预算、满意度、P95 和最大排队组合评分；
- 种子和规则可通过 URL 或分享码复现。

前置条件：完整进度存档和设计导入/导出已经稳定。

### 2. 五日扩建的策略分支 — ⬜

当前付费区域已提供成长节奏，后续可让区域选择产生真正取舍：

- 两个区域同时可选但短期预算只能开放其一；
- 不同区域带来不同需求、收入上限或公交机会；
- 后续条件根据已开放区域变化，但必须保持可解释和可验证；
- 参考答案与关卡测试覆盖每条受支持的关键分支。

不应引入依赖随机数的不可复现结果，也不应把教学关变成长时间等待任务。

### 3. 可选声音与视觉主题 — ⏸

候选内容包括施工、贯通、到站和结算音效，以及深色/高对比主题。所有声音必须默认克制、可独立静音，不得作为唯一反馈；视觉主题必须保持路线符号和文字对比度。

# 技术路线

## P1：降低空闲渲染与大地图开销 — ⬜

当前玩家端即使处于静止规划状态，也持续通过 `requestAnimationFrame` 执行完整 `updateUI()` 和 Canvas 重绘；运行中的 64×64 可访问网格也可能周期性更新大量 ARIA 标签。

计划：

- 规划静止时改为事件驱动重绘；
- 运营、视图回弹和短动画期间才保持连续帧；
- DOM 状态、Canvas 和可访问网格分别使用变更签名；
- 可访问网格只更新状态变化的格子，保留完整键盘导航语义；
- 建立 16×12 与 64×64 的可重复性能基线。

验收标准：静止规划时不持续执行完整地图绘制；大地图功能和无障碍播报无回归。

## P1：继续拆分玩家端主模块 — ⬜

`src/game/game.js` 已承担状态、输入、Canvas 绘制协调、存档、关卡导航、公交、信号、结算和管理员桥接等多种职责。

建议按现有无构建、浏览器全局与 CommonJS 约束逐步拆分：

- `game-input.js`：鼠标、触摸和键盘操作；
- `game-storage.js`：进度、设计、星级与迁移；
- `game-navigation.js`：章节、关卡和五日进度；
- `game-inspector.js`：道路、信号和公交检查器；
- 保持模拟规则位于 `src/shared/`，模块不得反向访问 DOM。

每次拆分必须保持脚本加载顺序、游戏服务器白名单和 Node/浏览器测试同步更新。

## P2：减少规则文案重复 — ⬜

帮助弹窗、README 和游戏指南重复维护大量数值与快捷键，容易产生漂移。

计划：

- 帮助弹窗缩减为短操作入口；
- 完整规则只在游戏指南集中维护；
- 可稳定结构化的道路数值、快捷键和功能开放信息尽量由共享常量渲染或由测试校验；
- 不引入模板引擎、打包器或外部依赖。

# 测试与发布要求

每项改进除功能本身外，必须同步满足：

1. 更新 `README.md`、`src/game/manual.html`；共享规则或管理员配置变化时同时更新 `src/admin/admin-manual.html`。
2. 修改关卡、预算、时限、需求或交通规则时，运行完整 `node --test tests/*.test.js` 并验证参考方案。
3. 涉及 DOM、Canvas、触摸、响应式布局、对话框、存档或 CSP 时，尽量运行玩家浏览器与 UI 冒烟测试。
4. 新增浏览器资源时更新 `src/server/game-server.js` 白名单、MIME 类型和服务器测试。
5. 存档变化必须有版本、严格校验、原子加载和旧版本迁移测试。
6. 完成功能后将本文件对应状态改为 ✅；部分实现只能标记为 🟡，并明确剩余验收项。

## 建议实施顺序

1. 修正文档快捷键并建立一致性检查。
2. 实现最近关卡、自动草稿和完整五日进度存档。
3. 增加短步骤情境教学与推荐下一课。
4. 建立可点击的结算热点和累计热力图。
5. 改善沙盒复制与实验参数。
6. 实现设计导入/导出。
7. 优化静止渲染和大地图无障碍更新。
8. 在上述基础稳定后，再评估确定性挑战变体与五日策略分支。
