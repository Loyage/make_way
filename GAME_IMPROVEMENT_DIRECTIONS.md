# 慢行小城：后续改进方向

本文是本轮全项目排查后形成的产品与技术路线建议。原文提到的 `LOGIC_AUDIT_DECISIONS.md` 当前仓库中不存在；已经落地的玩法以游戏指南和测试为准。

## 当前实施盘点

| 方向 | 状态 | 当前实现 / 尚缺内容 |
| --- | --- | --- |
| P0 运营前检查 | ✅ 完成 | 已检查不可达住宅、容量、公交有效性、误降级、信号冲突和理论送达上限，并支持地图定位。 |
| P0 路径解释 | ✅ 完成 | 住宅检查器显示目的地、汽车路径、距离、自由流耗时、最低等级瓶颈和不能出发原因。 |
| P0 人数概念 | ✅ 完成 | 住宅与结算显示人口守恒，目的地显示剩余容量、预约和抵达。 |
| P1 寻路与分流 | ✅ 完成 | 车辆先按道路速度寻路，运营中每秒结合实时负载、受阻、路口等待与信号周期重估；仅在动态成本至少降低约 18% 时换路，并继续遵守偏好/汽车禁行。 |
| P1 公交系统 | ✅ 完成 | 已有多线路、站点客流、班距、双向停站和运营报告；两线在同一道路站格停靠时支持最多一次换乘，按班距与车程选择预计总时间较短的方案，并在首次上车时预约最终容量。 |
| P1 路口可视化 | ✅ 完成 | 已有阶段高亮、冲突明细、按车流建议，以及各入口等待/队列报告。 |
| P1 工具语义 | ✅ 完成 | 拖拽从道路起步时可预览并统一经过道路的等级；区域与路口间路线提供图标快捷操作，整段施工需确认。 |
| P1 撤销/重做 | ✅ 完成 | 道路、剪断、等级、信号和公交均进入 60 步设计历史。 |
| P1 移动端分层 | ✅ 完成 | 已有地图优先布局、底部抽屉、信息标签和触摸坐标反馈。 |
| P1 无障碍 | ✅ 完成 | 已有同步可访问网格、状态播报、键盘控制以及非颜色图形标记。 |
| P2 章节学习曲线 | ✅ 完成 | 已整理为两章十一关并逐步开放概念；第 10 关教授单线公交，第 11 关教授两线同站换乘，每关提供通关、满意度和效率三项独立星级目标，五日任务按天记录。 |
| P2 复盘指标 | ✅ 完成 | 结算已展示路线送达率、平均与 P95 通勤、最大住宅排队、前三道路热点、公交和路口报告，并按关卡保存及比较个人最佳。 |
| P2 模式分离 | ✅ 完成 | 挑战模式保留固定预算、时限与结算；沙盒沿用关卡预算但不限时间，并可在运营中安全改路、调整道路引导和信号，管理员另有人工通关发布门禁。 |
| 共享关卡校验 | ✅ 完成 | 校验已迁入 `src/shared/level-validation.js`，游戏加载、管理员 API 与 Node 共用；现在会一次收集跨章节、跨关卡及常用独立字段问题，并返回关卡、字段路径和地图格坐标。 |
| 初始道路 v2 | ✅ 完成 | 关卡已拆分为 `initialRoads: [{cell, grade}]` 与 `initialEdges: [[a,b]]`；加载器兼容并迁移旧版 `[a,b,grade]`。 |
| 管理员安全与可用性 | ✅ 完成 | 已改为默认回环监听、按来源限制失败登录，并支持 HTTPS 场景的 `Secure` Cookie；章节表单、参数批量修改后统一应用草稿预览、参考答案直接加载测试与多日答案链自动运行、结构化问题跳转、“验证并试玩”人工通关发布门禁和一键重启游戏服务均已完成。 |
| 测试扩展 | ✅ 完成 | 单元/服务器/玩家端与管理端浏览器冒烟测试覆盖主要回归；管理端交互、两线同站换乘的 UI 规划/候车/结算流程、运营中对话框状态恢复、确定性随机不变量、键盘-only、真实触摸拖动及 320～1440px 移动端/桌面高 DPR 截图均已覆盖。 |

本轮已完成管理员安全项中的默认回环监听、登录限速/短期封禁和可配置 `Secure` Cookie，将关卡校验迁入共享模块、接入游戏加载与结构化 API 响应，支持一次列出问题并跳转到对应关卡或地图格，同时完成初始道路 v2 数据迁移、管理员“验证并试玩”人工通关发布门禁及其浏览器自动化流程、普通关卡与多日任务逐日参考答案、完整复盘指标、独立星级挑战、挑战/沙盒模式分离、动态拥堵寻路、公交跨线换乘、确定性属性测试和多输入/多尺寸浏览器回归。

## 一、优先级 P0：先让规则可理解、结果可信

### 1. 增加“运营前检查”

开始运营前给出不阻止玩家、但足够明确的检查结果：

- 哪些住宅没有可达的同色目的地；
- 哪些有限容量目的地不足以接收对应路线的居民；
- 哪些公交线路未闭环、没有有效上下客站，或站序无法把住宅送到目的地；
- 哪些主路被一次拖拽意外降级；
- 当前理论最多可送达人数是否低于关卡目标。

这样能减少“按了开始但不知道为什么失败”的挫败感。

### 2. 在地图上解释路径选择

选中住宅后显示：

- 当前分配的目的地；
- 小汽车预计采用的路径；
- 路径距离、自由流耗时和主要瓶颈；
- 无法出发时的具体原因（未连接、出口满、目的地满、线路不服务等）。

这比只显示“待连接/已连接”更能帮助玩家学习交通规划。

### 3. 区分不同“人数”概念

目前住宅总人口、已产生、排队、已离开、公交提前接走和目的地已分配都存在，但界面容易混淆。建议统一为一条守恒关系：

`总人口 = 尚未产生 + 住宅等待 + 小汽车在途 + 公交在途 + 已抵达`

在住宅检查器和结算页展示这条拆分，目的地则展示“剩余容量 / 已预约 / 已抵达”。

## 二、优先级 P1：提升规划深度

### 1. 更合理的寻路与分流

在确认 `LOGIC_AUDIT_DECISIONS.md` 的路线策略后，可加入：

- 按道路速度计算的基础路线成本；
- 拥堵、路口等待和信号周期的动态成本；
- 路线切换的迟滞，避免车辆每帧来回改道；
- 可选的玩家“道路偏好/禁行”工具，允许主动引导分流。

### 2. 公交从“画一条线”升级为完整系统

建议逐步加入：

- 站点客流预估、候车人数和上下车统计；
- 发车间隔或车辆间距，而不只是同时投放 1～3 辆车；
- 往返两端都可设置停站，而不是返程固定不停站；
- 多线路在共同启用的同一道路站格支持一次换乘；乘客综合班距与两段车程选择预计总时间较短的方案，并在首次上车时预约最终容量；
- 线路运营报告：周转时间、平均载客率、满载拒载人数。

### 3. 路口控制可视化

手动灯序已有较完整的逻辑，下一步应降低理解门槛：

- 编辑阶段时直接在地图路口高亮本阶段动作；
- 显示冲突原因，而不只提示“不能保存”；
- 提供“按当前车流生成建议灯序”；
- 运营报告列出各入口平均等待时间和最大队列。

## 三、优先级 P1：改善操作与界面

### 1. 简化工具语义

拖拽语义已收敛为“起点决定是否继承等级”：从道路起步时，以起点等级新建空地道路并改造经过的不同等级道路，用格内 ＋ / ↑ / ↓ 和顶部状态即时预告松手结果；从建筑或空地起步时只建设支路、补充连接并保留已有道路等级。拆除不由拖拽触发，仍需在选择模式明确操作。

### 2. 增加撤销/重做

规划操作天然适合命令栈：

- 道路绘制、剪断、批量升降级、信号设置、公交编辑均作为一次原子命令；
- 支持 `Ctrl/Cmd + Z` 与 `Ctrl/Cmd + Shift + Z`；
- 开始运营前保留历史，停止运营后仍可继续撤销规划；
- 读取存档、重置和切关作为明确的历史边界。

### 3. 移动端信息分层

小屏幕上不要同时展开所有长说明。建议：

- 地图和核心运营按钮固定在首屏；
- 检查器、公交和信号设置改为底部抽屉；
- 任务、客流、连接状态改为可折叠标签页；
- 增大 Canvas 格子命中反馈，并显示当前手指经过的格坐标。

### 4. 无障碍增强

Canvas 目前有键盘操作，但屏幕阅读器只能听到总说明。建议增加与地图同步的可访问网格或状态区，逐格播报地形、道路等级、连接方向、车辆负荷和选中状态；同时为颜色路线补充纹理/符号，避免只依赖颜色区分。

## 四、优先级 P2：关卡与长期目标

### 1. 让章节真正形成学习曲线

每章建议采用：

1. 单一概念教学；
2. 有明显错误的修复题；
3. 多解法综合题；
4. 有评分目标的挑战题。

通关条件仍用送达人数，附加星级可基于预算、满意度和最高排队长度，但不要把唯一解写死。

### 2. 增加可复盘指标

结算除满意度外，建议展示：

- 每条路线送达率；
- 平均/95 分位通勤时间；
- 最大住宅排队；
- 最拥堵的 3 个道路格或路口；
- 公交载客率与贡献人数；
- 与本关个人最佳设计的差异。

### 3. 沙盒与挑战分离

- **教学/挑战模式**：固定预算、时间和目标，可记录通关与星级；
- **沙盒模式**：不限时间，可实时修改，便于观察网络演化；
- **关卡编辑器预览**：管理员保存前可直接运行当前草稿，自动检查可达性与基本可通关性。

## 五、工程与管理面板

### 1. 共享一套关卡校验

现在核心加载与管理员保存的校验深度不同。建议把纯数据校验移入独立、同时支持浏览器和 CommonJS 的模块，由以下位置共同调用：

- 游戏通过路径清单加载 `built-in-levels.json` / `levels.json`，每关数据位于对应章节目录；
- 管理员保存；
- Node 关卡测试；
- 未来的关卡预览器。

校验结果应返回带章节、关卡和字段路径的结构化错误，而不是只有一段字符串。

### 2. 改进初始道路数据模型

`initialEdges: [a,b,grade]` 同时表达连接和道路等级，多个边共享一个格时会产生“后写入的边覆盖格子等级”的顺序语义。建议在下一版关卡格式中拆成：

- `initialRoads: [{cell, grade}]`
- `initialEdges: [[a,b]]`

并提供 v1 → v2 迁移。这样管理器显示、预算计算和核心最终状态会完全一致。

### 3. 管理员安全与可用性

- 默认仅监听回环地址，远程使用时显式开启；
- 登录接口增加按来源限速和短期封禁；
- 经反向代理提供 HTTPS 后再启用 `Secure` Cookie；
- 用页面内章节编辑表单替代连续 `prompt()`；
- 保存前列出全部校验问题，并能点击跳转到对应字段/地图格；
- 增加“验证并试玩”，避免无解关卡进入玩家覆盖层。

## 六、测试策略

### 保留的测试重点

- 确定性模拟、容量预约、路口冲突、公交上下客与预算；
- 存档原子加载和旧版本迁移；
- 每关参考规划可通关；
- 服务白名单、认证和路径安全；
- 鼠标、触摸、键盘三种输入的一致性。

### 建议新增

- 已对多组确定性随机规划和合法路网运行属性测试：覆盖预算、道路/连接合法性与操作原子性、存档往返、人数守恒、目的地容量、车道预约不重叠及动态成本下界；
- 管理面板浏览器测试已覆盖登录、快速拖画无断路、删除/移动建筑自动清理初始道路、未保存重载保护、星级编辑、发布阻止、试玩通关、版本指纹失效、撤销恢复、发布解锁与一键重启游戏服务；
- 游戏浏览器测试已覆盖运营中打开帮助、重置、模式、换关和停止对话框时暂停计时，取消后恢复运营，以及确认停止后清空运营进度但保留设计；
- 已在 320～1440px 六档视口执行内存截图检查，验证移动端/桌面高 DPR 的物理像素尺寸、Canvas 像素密度、画面颜色细节与非空覆盖；玩家浏览器测试同时覆盖键盘-only 的建路、选择、撤销/重做、取消/开始运营，以及真实触摸拖动、坐标反馈和撤销历史。

### 本轮已清理

- 删除了 `tests/traffic.test.js` 中与核心“终局状态禁止修改”覆盖重复的测试；
- 删除了测试文件中的未使用导入；
- 新增的测试只覆盖本轮实际修复的回归点，而不重复已有断言。
