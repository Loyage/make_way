# P1：设计导入、导出与分享

**状态：⬜ 待实施**

## 目标

在不增加服务器端账户的前提下，将当前设计导出为带版本的 JSON 文件或文本分享码，并支持从文件/文本导入后严格校验。

分享内容只包含规划数据，不包含成绩、车辆或管理员关卡定义。导入前预览关卡 ID、道路数、公交线数和预计费用。

## 验收

合法设计可跨浏览器往返；恶意或损坏输入不会部分修改当前城市。测试覆盖序列化、解析、往返和损坏输入拒绝。
