# Admin panel password. Used by deploy/install-admin-service.sh and
# admin-server.js (via ADMIN_PASSWORD or PI_ADMIN_PASSWORD environment var,
# with this file read by the install script). Change this to a strong secret.
ADMIN_PASSWORD=admin
