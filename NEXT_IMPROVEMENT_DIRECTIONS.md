# 慢行小城：下一阶段改进建议

本文承接 `GAME_IMPROVEMENT_DIRECTIONS.md`。上一阶段列出的运营前检查、路径解释、人数守恒、动态寻路、公交换乘、星级、复盘、管理员发布门禁及浏览器回归等事项均已完成；以下内容作为下一阶段候选路线，不表示已经实现。

## 优先级总览

| 优先级 | 方向 | 当前问题 | 建议结果 |
| --- | --- | --- | --- |
| P0 | 管理员部署暴露面 | 当前已安装的管理员服务监听所有网卡，日志明确提示仅由密码保护 | 默认只允许本机访问；确需远程管理时显式配置网络入口和安全措施 |
| ✅ P1 | 内置公交换乘教学 | 已新增第 11 关「同站换乘」，在单线教学后开放两条线路 | 玩家可按提示规划两线、启用共同站格并观察换乘 |
| ✅ P1 | 换乘浏览器端回归 | 已从 UI 覆盖两线绘制、共享站、换乘候车和结算报告 | 浏览器测试隔离注入内置目录，不写真实覆盖关卡 |
| ✅ P1 | 多日任务参考答案 | 已在每个 `campaign.days[]` 内支持独立 `referenceDesign` | 玩家按日期解锁，管理员在实际试玩进度中逐日捕获 |
| P2 | 公交预计时间精度 | 当前主要使用半个班距与静态车程估算 | 运营中结合实时车辆位置、停站、拥堵和满载情况预测 |
| P2 | 公交换乘规划性能 | 行程查询会枚举线路、站点和目的地 | 缓存线路拓扑和换乘候选，避免规模扩大后的重复计算 |
| P2 | 视觉回归精度 | 截图测试能发现空白、尺寸和 DPR 问题，但不比较稳定视觉基线 | 为少量关键界面增加稳定区域快照或布局几何断言 |
| P2 | 路线文档整理 | 旧路线文件中的全部项目已经完成，但正文仍以建议语气保留 | 将旧文件归档为完成记录，以本文维护新阶段状态 |

## 一、P0：收紧管理员部署暴露面

### 当前问题

管理员服务器代码已经默认绑定回环地址，但当前已安装的 `traffic-game-admin.service` 仍监听 `[::]:8080`。服务启动日志提示管理面板可从网络访问，并且密码是唯一防线。

### 建议

- 本机管理场景将 `ADMIN_HOST` 固定为 `127.0.0.1` 或 `::1`；
- 只有明确需要局域网或公网访问时才监听所有网卡；
- 远程管理应放在 HTTPS 反向代理后，并启用 `Secure` Cookie；
- 保留现有登录限速和短期封禁；
- 在部署手册中明确区分“仅本机”和“远程管理”配置；
- 修改 systemd unit 或重新运行安装脚本前必须先征得用户确认。

### 验收标准

- 默认安装后，其他主机无法直接连接管理员端口；
- `systemctl --user status traffic-game-admin.service` 不再出现网络暴露警告；
- 管理员服务器认证、限速和浏览器测试继续通过；
- 游戏公开服务不受管理员监听地址变更影响。

## 二、✅ P1：增加内置公交换乘教学

### 实施结果

已保留第 10 关作为单线公交教学，并新增第 11 关「同站换乘」。新关卡开放两条线路，以住宅—中央站—学校的布局、任务说明和提示引导玩家在中央同一道路格启用两线站点；根据用户选择，不增加强制换乘人数通关条件。参考规划使用两条原路返回线路，每线配置两辆公交车，在预算和时限内可获得全部三星。

### 需要同步

- 新关卡 JSON、章节清单和解锁顺序；
- `src/game/manual.html` 的关卡说明；
- `README.md` 的关卡列表与玩法描述；
- 管理员手册中的换乘关卡编辑说明；
- 关卡校验和参考规划测试；
- 星级目标与难度平衡。

### 验收结果

- 玩家可按任务提示完成一次同站换乘；
- 两条线路使用相邻但不同站格时不能错误换乘；
- 正确共享站方案不会违反最终目的地容量；
- 一名乘客不会进行第二次换乘；
- 完整 Node 关卡测试证明参考规划可通关并获得三星。

## 三、✅ P1：补充公交换乘端到端浏览器测试

### 实施结果

`tests/bus.test.js` 继续验证核心行程选择、共享站、一次换乘、首次上车容量预约、换乘等待、统计和人口守恒；`tests/browser-smoke.cjs` 现已通过玩家控件绘制两条线路、启用共同站格，并验证换乘候车与结算统计。测试会在浏览器内注入内置目录，避免受本机 `levels.json` 覆盖目录影响。

### 建议场景

在隔离的测试关卡或浏览器内注入的测试目录中执行：

1. 选择第一条公交线路并绘制路线；
2. 新建第二条线路并绘制路线；
3. 在同一道路格分别启用两线站点；
4. 检查站点面板显示可换乘线路和预计客流；
5. 开始运营并等待至少一名乘客完成换乘；
6. 检查顶部状态区分住宅候车与换乘候车；
7. 检查结算报告显示换入、换出、换乘次数和等待时间；
8. 删除其中一条线路的共享站，确认运营前检查指出服务失效。

### 测试隔离要求

- 不修改真实 `levels.json` 或 `levels.local/`；
- 不依赖浏览器已有 `localStorage`；
- 使用真实 CDP 鼠标、键盘或触摸事件完成关键操作；
- 测试结束后关闭临时页面并恢复模拟状态；
- 保持确定性，不依赖随机发车或墙钟竞态。

### 验收标准

- 测试能在现有无第三方依赖环境运行；
- 核心换乘逻辑失效、界面统计遗漏或共享站判断错误时测试会失败；
- 玩家端完整浏览器测试和多尺寸 UI 测试继续通过。

## 四、✅ P1：支持多日任务参考答案

### 实施结果

共享校验器、玩家端和管理员面板已统一采用逐日参考设计；内置五日任务每天均配置可通关答案。失败解锁键包含日期，载入答案会回到当前日运营前检查点，不会重置已完成日期、累计收入或预算；实际预算不足时保持进度并拒绝载入。

### 数据模型

每个日期直接保存可选参考设计：

```json
{
  "campaign": {
    "days": [
      {
        "referenceDesign": {
          "version": 2,
          "roads": [],
          "edges": [],
          "signals": [],
          "busLines": []
        }
      }
    ]
  }
}
```

旧 campaign 不需要迁移；未配置 `referenceDesign` 的日期维持原行为。

### 已实现行为

- 每一天的参考答案基于该日已解锁建筑、预算和累计进度；
- 载入参考答案只替换规划，不自动运营；
- 不覆盖已完成天数、收入、星级和个人最佳；
- 管理员只能从对应日期的真实试玩状态捕获答案，捕获后可继续推进后续日期；
- 修改 campaign 条件会使试玩认证失效；
- 校验器按日期检查格式、地图、当日建筑、最高累计预算、公交和信号配置。

### 验收结果

- 管理员可逐日保存、删除、验证并试玩参考答案；
- 玩家失败后只能查看当前日期的参考答案；
- 每日答案均通过参考规划和浏览器回归验证；
- 旧 campaign 数据无需迁移即可继续加载。

## 五、P2：提高公交预计时间精度

### 当前问题

规划阶段按首段半个班距、两段静态车程和第二段半个班距比较方案。该模型确定、易解释，但运营中没有考虑：

- 公交与站点的实时距离；
- 前方拥堵和信号等待；
- 站点上下客停留；
- 车辆当前载客量和满载拒载概率；
- 换乘线路车辆是否刚刚离站。

### 建议分阶段实现

1. **规划阶段**继续使用静态可解释模型；
2. **运营阶段**根据线路上最近可服务车辆估算首次等待；
3. 将道路动态成本、信号周期和预计停站时间加入车程；
4. 满载时提高该班次成本，但不要取消已有最终容量预约；
5. 对动态换线增加迟滞，避免候车乘客频繁改变计划；
6. 已上车乘客锁定剩余行程，除非线路被运营中安全编辑破坏。

### 验收标准

- 相同状态下行程选择保持确定性；
- 高频但严重拥堵线路不再总是优于畅通的低频线路；
- 车辆刚到站时，实时预测优于简单的半班距估算；
- 不引入乘客重复、容量超订或路线抖动。

## 六、P2：优化公交行程查询性能

### 当前问题

`busItineraryFor()` 会枚举线路、上车位置、目的地、共享站和第二段停靠位置。当前地图固定为 16 × 12、线路数量较少，性能足够；如果管理员创建更多线路和站点，界面客流预测及运营上车检查会重复计算相同拓扑。

### 建议

- 按线路版本缓存运营路线和站点位置；
- 预先构建“站格 → 线路与方向位置”索引；
- 预先构建最多一次换乘的候选拓扑；
- 将静态车程与动态运行成本分离缓存；
- 道路、线路、站点、往返设置或班距变化时精确失效；
- 容量变化只重新筛选目的地，不重建线路拓扑。

### 验收标准

- 缓存前后同一输入产生完全相同的确定性结果；
- 存档加载、撤销/重做和运营中安全编辑不会读取过期缓存；
- 增加包含多线路和多站点的性能基准或有界调用次数测试；
- 不为当前小规模场景引入难以维护的全局状态。

## 七、P2：增强视觉回归精度

### 当前问题

`tests/ui-smoke.cjs` 已覆盖六档视口、移动端和桌面高 DPR、Canvas 像素密度、PNG 非空及颜色细节，但它不维护像素基线，因此细微错位、遮挡或字体换行变化可能无法发现。

### 建议

- 优先增加稳定的 DOM 几何断言，而不是维护整页像素截图；
- 对地图首屏位置、工具栏遮挡、对话框边界、抽屉高度和触控目标间距设置容差；
- 仅为少量稳定区域保存裁剪后的视觉基线；
- 基线应固定字体和 DPR，并允许小范围抗锯齿差异；
- 不提交每次运行生成的临时截图或浏览器配置目录。

### 验收标准

- 能发现地图被工具栏遮挡、对话框超出视口和按钮重叠；
- 在支持的 Chromium 版本中稳定运行；
- 不因普通抗锯齿差异产生大量误报；
- 继续保持测试无 npm 依赖。

## 八、P2：整理路线文档

### 当前问题

`GAME_IMPROVEMENT_DIRECTIONS.md` 的实施盘点已经全部完成，但后续章节仍保留“建议新增”“下一步”等历史语气，容易让读者误以为这些功能尚未实现。

### 建议

- 将旧文件标题或开头改为“上一阶段改进记录”；
- 保留完成状态、设计决策和历史验收信息，不删除可追溯内容；
- 以后只在本文维护下一阶段状态；
- 每项完成后同步更新状态、实际实现和测试入口；
- 新发现的问题应先写清用户影响、范围和验收标准，再进入开发。

## 推荐实施顺序

1. 先确认并收紧管理员服务监听地址；
2. ~~设计内置公交换乘教学关；~~（已完成）
3. ~~为该关补充核心参考规划和浏览器端完整换乘回归；~~（已完成）
4. ~~确定多日参考答案的数据模型后再实现；~~（已完成）
5. 根据实际性能数据决定是否缓存公交拓扑；
6. 最后补充实时到站预测和更精细的视觉基线。

## 通用验证要求

每项改动至少执行与范围对应的检查：

```sh
node --check src/shared/core-bus.js
node --check src/shared/core.js
node --check src/game/game.js
node --check src/admin/admin.js
node --test tests/*.test.js
```

涉及玩家 DOM、Canvas、触摸、键盘、对话框或公交交互时，应运行：

```sh
node tests/browser-smoke.cjs
node tests/ui-smoke.cjs
```

涉及管理员工作流时，应运行：

```sh
node tests/admin-browser-smoke.cjs
```

新增或修改玩家规则时同步更新 `src/game/manual.html`；管理员工作流同步更新 `src/admin/admin-manual.html`；项目说明和完成状态同步更新 `README.md` 与本文。
