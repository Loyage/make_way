# 游戏逻辑排查：待确认的设计选择

> 本文件只记录无法在不改变玩法取向的前提下擅自决定的问题。请直接编辑每项下的“你的决定”。未确认前，我保留现状；确定无歧义的 bug 已继续修复。

## 1. 小汽车如何选择路线

**现状**：只选择经过格数最少的路径，不考虑道路等级、信号灯、拥堵和预计耗时。因此更长但明显更快的快速路可能始终无人使用，玩家修建分流也可能没有效果。

**可选方案**：

- A（建议）：按动态预计耗时寻路，计入道路速度、当前负荷和信号等待；车辆出发后允许在节点重新选择。
- B：只按道路自由流耗时寻路（计入道路等级，不计实时拥堵），更稳定、可预测。
- C：保持“最少格数”，继续把它作为需要玩家适应的明确规则。

**你的决定**：`待填写`

## 2. 公交是否绕过住宅客流产生速率

**现状**：公交到站后可立即从住宅总人口中装客，即使这些居民按住宅的每秒流量本来尚未产生；小汽车则必须等待流量逐步产生。这让公交教学关可行，但“输出 X 人/s”的含义因交通方式而异。

**可选方案**：

- A：公交也只能接走已产生并排队的乘客；需要重调公交关卡时限、目标或住宅流量。
- B（建议）：保留公交优先聚客能力，但把字段拆成“居民产生率”和“小汽车出口能力”，界面明确说明公交只绕过后者。
- C：保持现状，仅将其视为公交优势。

**你的决定**：`待填写`

## 3. 拖拽经过高级道路时是否自动降级

**现状**：从支路或建筑开始建设，笔迹经过既有干道/快速路时，会把其降成起点等级并退款。该规则虽已写入指南，但很容易因补一条出口而意外破坏主路。

**可选方案**：

- A（建议）：建设拖拽只铺新路和补连接，不自动降低既有道路；升级仍可随笔迹提高等级，降级必须使用选择区的“降级”按钮。
- B：既有道路等级完全不随拖拽变化，所有升降级只通过选择区按钮。
- C：保持当前“整条笔迹统一为起点等级”的规则。

**你的决定**：`待填写`

## 4. 失败结算中的满意度

**现状**：满意度只统计已经抵达的居民。失败时即使大多数人滞留，只要少数抵达者很快，也可能显示 100%。本轮已先把界面名称改为“已抵达居民满意度”，避免误导。

**可选方案**：

- A（建议）：未抵达且已经产生的居民按最低档计入满意度。
- B：关卡全部住宅人口中未抵达者都按最低档计入。
- C：保持只统计抵达者，并保留现在更准确的名称。

**你的决定**：`待填写`

## 5. 关卡是否需要逐步解锁

**现状**：章节和“教学一～八”营造了进度感，但所有关卡可以自由进入；“完成前面的教学关卡后解锁”类提示实际是按当前关卡功能开关决定，并不记录通关进度。

**可选方案**：

- A：保存通关进度，默认逐关解锁，并提供“自由模式/全部解锁”。
- B（建议）：保持自由选择，把所有“完成前面后解锁”措辞改成“本关未开放”，避免伪进度暗示。
- C：保持现状。

**你的决定**：`待填写`

## 6. 管理员面板的网络暴露策略

**现状**：管理员服务默认监听所有网卡，且没有 TLS 或登录频率限制。此次已移除内置 `admin` 默认密码：未显式设置密码时服务会拒绝启动，安装脚本也拒绝弱默认值。

**可选方案**：

- A（建议）：默认只监听 `127.0.0.1`，需要远程管理时由用户显式改成 `::`，并建议经 SSH 隧道访问。
- B：继续默认监听所有网卡，增加登录限速/临时封禁。
- C：保持当前网络策略，仅依赖强密码。

**你的决定**：`待填写`
